```mermaid
graph TD
    A[Client] --> B{GET /api/v1/wells/well/{well_code}};
    A --> C{GET /api/v1/wells/field/{field_code}};
    B --> D[WellProductionQueryService];
    C --> D;
    D -- Get by Well Code / Field Code --> E{WellProductionRepository Port};
    E --> F[DuckDBWellProductionRepository Impl];
    F -- Query Data --> G[DuckDB Database];
    G -- Queried WellProduction Entities --> F;
    F -- WellProduction Entities --> E;
    E -- WellProduction Entities --> D;
    D -- Formatted Data --> H[API Response];
    H --> A;

    subgraph "API Interface (src/interfaces/api)"
        B; C;
    end

    subgraph "Application Service (src/application/services)"
        D;
    end

    subgraph "Domain Repository Port (src/domain/repositories)"
        E;
    end

    subgraph "Infrastructure Repository (src/infrastructure/repositories)"
        F;
    end

    subgraph "Infrastructure DB (src/infrastructure/db)"
        G;
    end

    subgraph "API Response Handling"
        H;
    end

    style A fill:#lightgrey,stroke:#333,stroke-width:2px
    style G fill:#lightgrey,stroke:#333,stroke-width:2px
```
